/// @description Insert description here
// You can write your code in this editor





// Inherit the parent event

if(slot >= 0 && if_able)
{
	draw_sprite_ext(bsprite, 0, x, y, image_xscale, image_yscale, 0, c_white, 1);
	
	
}

else if(slot >= 0 && !if_able)
{
	draw_sprite_ext(bsprite, 0, x, y, image_xscale, image_yscale, 0, c_grey, 1)

}