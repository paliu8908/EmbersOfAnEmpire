/// @description Insert description here
// You can write your code in this editor

//re-evaluate how it is calculated
/*if(other.if_ready && other.faction_number != faction_number)
{
	hp -= (other.dmg - dmg_reduction);
	
	if(other.direction > direction)
	{
		var atkAngle = other.direction - direction;
	}
	else
	{
		var atkAngle = other.direction - direction + 360;
	}
	
	randomize();
	
	var ifHit = random(100);
	
	if(atkAngle > 60 && atkAngle < 90 && ifHit == 1)
	{

		reloadTime = 10;
	}
	
	else if(atkAngle > 270 && atkAngle < 300 && ifHit == 1)
	{
		reloadTime = 10;
	}
	
	else if(atkAngle > 90 && atkAngle < 120 && ifHit == 1)
	{
		move_speed = 1;
	}
	
	else if(atkAngle > 240 && atkAngle < 270 && ifHit == 1)
	{
		move_speed = 1;
	}
	
	else if(atkAngle > 150 && atkAngle < 210 && ifHit == 1)
	{
		hp -= (dmg - dmg_reduction);
	}
}*/